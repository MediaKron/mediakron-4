import Layout from '../views/layouts/Layout';
import Browse from '../views/items/Browse';

export default {
    path: '/:site',
    component: Layout,
    title: 'Site',
    props: true,
    children: [
      
    ],
}
