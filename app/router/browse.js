import Layout from '../views/site/SiteLayout';
import Browse from '../views/site/items/Browse';

export default {
    path: '/:site',
    component: Layout,
    title: 'Site',
    props: true,
    children: [
      
    ],
}
