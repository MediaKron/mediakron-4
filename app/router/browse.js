import Layout from '../views/site/SiteLayout';
import Content from '../views/site/items/Content';

export default {
    path: '/:site',
    component: Layout,
    title: 'Site',
    props: true,
    children: [
      
    ],
}
